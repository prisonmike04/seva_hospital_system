/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Scans all files in the src folder
    "./public/index.html", // Optionally, include public files if required
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
